<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.smart.frame">

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS" tools:ignore="ProtectedPermissions" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.fetures.MainActvitiy">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 初始化service -->
        <service android:name=".app.InitializeService" />

        <!-- 登录 -->
        <activity android:name=".ui.fetures.user.activity.LoginActivity" />
        <!-- 注册 -->
        <activity android:name=".ui.fetures.user.activity.RegisterActivity" />
        <!-- 忘记登录密码 -->
        <activity android:name=".ui.fetures.user.activity.ForgetLoginPwdActivity" />
        <!-- 重置登录密码 -->
        <activity android:name=".ui.fetures.user.activity.ResetLoginPwdActivity"/>
    </application>

</manifest>